import React from 'react';
import ReactDOM from 'react-dom/client';
import TresorApp from './TresorApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TresorApp />
  </React.StrictMode>
);
